@page "/videos"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>Videos - YouTube Video Tracker</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to the new video library page
        Navigation.NavigateTo("/video-library", replace: true);
    }
}
